---
- name: Install maria db
  apt:
    name: 
      - python3-mysqldb
      - mariadb-server
    update_cache: yes

# - name: Set the root password
#  mysql_user:
#    name: root
#    password: "{{ database.root_password }}"
#    login_unix_socket: /run/mysqld/mysqld.sock

# - name: Login as root user and grant permissions to ptg user
#  mysql_user:
#    login_unix_socket: /run/mysqld/mysqld.sock
#    login_user: root
#    login_password: "{{ database.root_password }}"
#    name: "{{ run.user }}"
#    password: "{{  database.ptg_user_password }}"
#    priv: '*.*:ALL'
#    state: present

- name: Give permissions to directory 
  ansible.builtin.file:
    path: /var/lib/mysql
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

# - name: Give permissions to directory 
#  ansible.builtin.file:
#    path: /etc/mysql/mariadb.conf.d
#    state: directory
#    recurse: yes
#    owner: "{{ run.user }}"
#    group: "{{ run.user }}"

- name: Give permissions to directory 
  ansible.builtin.file:
    path: /var/log/mysql
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

- name: Give permissions to directory 
  ansible.builtin.file:
    path: /var/log/private
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"
        
- name: Give permissions to directory 
  ansible.builtin.file:
    path: /usr/bin
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

- name: Give permissions to directory 
  ansible.builtin.file:
    path: /temp
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

- name: Give permissions to file 
  ansible.builtin.file:
    path: /run/mysqld
    state: directory
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

